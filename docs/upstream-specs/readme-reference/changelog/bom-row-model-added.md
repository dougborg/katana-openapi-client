# BOM row model added

Added [BOM row endpoints](https://developer.katanamrp.com/reference/bom-row), that replace the existing [recipe](https://developer.katanamrp.com/reference/recipe) ones. Recipe endpoints are now deprecated, and the breaking changes described in previous post have been implemented.